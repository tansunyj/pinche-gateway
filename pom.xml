<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.example</groupId>
  <artifactId>silievo-api-gateway-service</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>pinche</name>
  <url>http://maven.apache.org</url>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.4</version>
    <relativePath/>
  </parent>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Override Lombok version for Java 25 compatibility -->
    <lombok.version>1.18.42</lombok.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
      <exclusions>
        <!-- 排除默认的 Logback，改用 Log4j2 -->
        <exclusion>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- 使用 Log4j2 作为日志实现（与 @Log4j2 注解配套） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-log4j2</artifactId>
    </dependency>

    <!-- Reactor Context → 线程局部 MDC 的桥接库（自动上下文传播）
         WebFlux 是响应式模型，一条请求会跨多个线程执行，普通 ThreadLocal（MDC）在算子切换线程后丢失。
         micrometer context-propagation 让 Reactor 在每个算子执行边界自动把 Context 中的键
         同步到 Log4j2 ThreadContext，日志 pattern 用 %X{requestId} 即可让全链路日志自动带请求ID。
         版本号由 Spring Boot 3.3.4 的 micrometer-bom(1.13.4) 统一管理（→ 1.1.1）。 -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>context-propagation</artifactId>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- R2DBC 全反应式数据库支持 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-r2dbc</artifactId>
    </dependency>

    <!-- R2DBC MySQL 驱动 -->
    <dependency>
      <groupId>io.asyncer</groupId>
      <artifactId>r2dbc-mysql</artifactId>
      <version>1.1.3</version>
    </dependency>

    <!-- Redis 反应式支持 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
    </dependency>

    <!-- 连接池支持 -->
    <dependency>
      <groupId>io.projectreactor.addons</groupId>
      <artifactId>reactor-pool</artifactId>
    </dependency>

    <!-- 阿里云 OSS SDK（生图 base64→URL，DashScopeImageAdapter 用；HTTP 上传接口已移除） -->
    <dependency>
      <groupId>com.aliyun.oss</groupId>
      <artifactId>aliyun-sdk-oss</artifactId>
      <version>3.17.4</version>
    </dependency>


  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <!-- 把当前激活的 Maven profile 对应的 Spring profile 传给 spring-boot:run
               （作为 spring.profiles.active 启动参数，命令行长横线前缀两个）：
               dev / prod-cn / prod-intl 在下方 profiles 中定义。
               IDEA 中：Maven 工具窗口 - Profiles 勾选环境后运行 spring-boot:run 即按所选环境启动。 -->
          <profiles>
            <profile>${profile.active}</profile>
          </profiles>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <!-- ============================================================
       多环境 Maven profiles（对应 src/main/resources/application-*.yml）
       IDEA Maven 工具窗口 - Profiles 勾选切换，再运行 spring-boot:run 即按所选环境启动：
         dev        开发环境（默认激活；勾选其他 profile 后 dev 自动取消）
       ============================================================ -->
  <profiles>
    <profile>
      <id>dev</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <profile.active>dev</profile.active>
      </properties>
    </profile>

  </profiles>
</project>
